name: Validate
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    name: KASA Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate evidence and documentation
        run: python3 scripts/validate.py
